{
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "rm -rf dist && observable build"
  },
  "dependencies": {
    "@observablehq/framework": "^1.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
